# Copy to .deploy.env (gitignored) and fill in — KryDeploy-style git-push deploy (optional).
VPS=root@YOUR_VPS_IP
KEY=~/.ssh/your_key
APP_DIR=/opt/krymark
URL=https://krymark.example.com
PROXY=nginx-proxy
APP_CT=krymark-app
